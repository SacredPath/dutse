{
  "name": "drainer-module",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "echo 'No build step required for Node.js app'",
    "test": "node run-tests.js",
    "test:quick": "node quick-test.js",
    "test:health": "node -e \"fetch('http://localhost:3000/health').then(r => r.json()).then(console.log).catch(console.error)\"",
    "test:validate": "node -c server.js && node -c api/*.js && node -c src/*.js && echo '✅ All files pass syntax validation'"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.13",
    "@solana/web3.js": "^1.98.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
